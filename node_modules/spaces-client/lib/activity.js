// Generated by CoffeeScript 1.6.3
(function() {
  var Activity, exports;

  Activity = (function() {
    var Faker, Resource;

    function Activity() {}

    Faker = require('faker');

    Resource = require('./resource');

    Activity.showActivity = function(site, sessionId, resourceId, parentId, userId, onsuccess, onfail) {
      var addlHeaders;
      addlHeaders = {
        "Cookie": ["_social_navigator_session=" + sessionId, "path=/"],
        "Accept": "application/vnd.moxiesoft.spaces-v1+json"
      };
      return Resource.getResource(site, userId, sessionId, 'activities/' + resourceId, addlHeaders, onsuccess, onfail);
    };

    Activity.createActivity = function(site, sessionId, resourceId, parentId, userId, onsuccess, onfail, oncreate) {
      var data;
      data = {
        activity: {
          status: Faker.Lorem.sentences(1)
        }
      };
      return Resource.createResource(site, userId, sessionId, 'activities', data, onsuccess, onfail, oncreate);
    };

    return Activity;

  })();

  module.exports = exports = Activity;

}).call(this);
