// Generated by CoffeeScript 1.6.3
(function() {
  var Blog, exports;

  Blog = (function() {
    var Faker, Resource;

    function Blog() {}

    Faker = require('faker');

    Resource = require('./resource');

    Blog.createBlog = function(site, sessionId, parentId, userId, onsuccess, onfail, oncreate) {
      var data;
      data = {
        blog: {
          title: Faker.Lorem.sentences(1),
          body: Faker.Lorem.sentences(4)
        }
      };
      if (parentId) {
        data['blog']['parent_id'] = parentId;
      }
      return Resource.createResource(site, userId, sessionId, 'blogs', data, onsuccess, onfail, oncreate);
    };

    Blog.updateBlog = function(site, sessionId, refObjId, userId, onsuccess, onfail) {
      var data;
      data = {
        blog: {
          title: Faker.Lorem.sentences(1)
        }
      };
      return Resource.editResource(site, userId, sessionId, 'blogs/' + refObjId, data, onsuccess, onfail);
    };

    Blog.deleteBlog = function(site, sessionId, refObjId, userId, onsuccess, onfail, ondestroy) {
      return Resource.deleteResource(site, userId, sessionId, 'blogs/' + refObjId, onsuccess, onfail, ondestroy);
    };

    Blog.createBlogpost = function(site, sessionId, blogId, userId, onsuccess, onfail, oncreate) {
      var data;
      data = {
        blog_post: {
          title: Faker.Lorem.sentences(1),
          body: Faker.Lorem.sentences(20),
          parent_id: blogId
        }
      };
      return Resource.createResource(site, userId, sessionId, 'blog_posts', data, onsuccess, onfail, oncreate);
    };

    Blog.updateBlogpost = function(site, sessionId, refObjId, userId, onsuccess, onfail) {
      var data;
      data = {
        blog_post: {
          body: Faker.Lorem.sentences(20)
        }
      };
      return Resource.editResource(site, userId, sessionId, 'blog_posts/' + refObjId, data, onsuccess, onfail);
    };

    Blog.deleteBlogpost = function(site, sessionId, refObjId, userId, onsuccess, onfail, ondestroy) {
      return Resource.deleteResource(site, userId, sessionId, 'blog_posts/' + refObjId, onsuccess, onfail, ondestroy);
    };

    return Blog;

  })();

  module.exports = exports = Blog;

}).call(this);
