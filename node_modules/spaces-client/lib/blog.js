// Generated by CoffeeScript 1.6.3
(function() {
  var Blog, exports;

  Blog = (function() {
    var Faker, Resource;

    function Blog() {}

    Faker = require('faker');

    Resource = require('./resource');

    Blog.showBlog = function(site, sessionId, resourceId, parentId, userId, onsuccess, onfail) {
      var addlHeaders;
      addlHeaders = {
        "Cookie": ["_social_navigator_session=" + sessionId, "path=/"],
        "Accept": "application/vnd.moxiesoft.spaces-v1+json"
      };
      return Resource.getResource(site, userId, sessionId, 'blogs/' + resourceId, addlHeaders, onsuccess, onfail);
    };

    Blog.createBlog = function(site, sessionId, resourceId, parentId, userId, onsuccess, onfail, oncreate) {
      var data;
      data = {
        blog: {
          title: Faker.Lorem.sentences(1),
          body: Faker.Lorem.sentences(4)
        }
      };
      if (parentId) {
        data['blog']['parent_id'] = parentId;
      }
      return Resource.createResource(site, userId, sessionId, 'blogs', data, onsuccess, onfail, oncreate);
    };

    Blog.updateBlog = function(site, sessionId, resourceId, parentId, userId, onsuccess, onfail) {
      var data;
      data = {
        blog: {
          title: Faker.Lorem.sentences(1)
        }
      };
      return Resource.editResource(site, userId, sessionId, 'blogs/' + resourceId, data, onsuccess, onfail);
    };

    Blog.deleteBlog = function(site, sessionId, resourceId, userId, onsuccess, onfail, ondestroy) {
      return Resource.deleteResource(site, userId, sessionId, 'blogs/' + resourceId, onsuccess, onfail, ondestroy);
    };

    Blog.showBlogpost = function(site, sessionId, resourceId, parentId, userId, onsuccess, onfail) {
      var addlHeaders;
      addlHeaders = {
        "Accept": "application/vnd.moxiesoft.spaces-v1+json",
        "Cookie": ["_social_navigator_session=" + sessionId, "path=/"]
      };
      return Resource.getResource(site, userId, sessionId, 'blog_posts/' + resourceId, addlHeaders, onsuccess, onfail);
    };

    Blog.createBlogpost = function(site, sessionId, resourceId, parentId, userId, onsuccess, onfail, oncreate) {
      var data;
      data = {
        blog_post: {
          title: Faker.Lorem.sentences(1),
          body: Faker.Lorem.sentences(20),
          parent_id: parentId
        }
      };
      return Resource.createResource(site, userId, sessionId, 'blog_posts', data, onsuccess, onfail, oncreate);
    };

    Blog.updateBlogpost = function(site, sessionId, resourceId, parentId, userId, onsuccess, onfail) {
      var data;
      data = {
        blog_post: {
          body: Faker.Lorem.sentences(20)
        }
      };
      return Resource.editResource(site, userId, sessionId, 'blog_posts/' + resourceId, data, onsuccess, onfail);
    };

    Blog.deleteBlogpost = function(site, sessionId, resourceId, parentId, userId, onsuccess, onfail, ondestroy) {
      return Resource.deleteResource(site, userId, sessionId, 'blog_posts/' + resourceId, onsuccess, onfail, ondestroy);
    };

    return Blog;

  })();

  module.exports = exports = Blog;

}).call(this);
