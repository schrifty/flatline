// Generated by CoffeeScript 1.6.3
(function() {
  var Blog, exports;

  Blog = (function() {
    var Faker;

    function Blog() {}

    Faker = require('Faker');

    Blog.create = function(site, parentId, userId) {
      var Spaces, XMLHttpRequest, data, xhr;
      Spaces = require('./spaces-client.js');
      data = {
        blog: {
          title: Faker.Lorem.sentences(1),
          body: Faker.Lorem.sentences(4)
        }
      };
      if (parentId) {
        data['blog']['parent_id'] = parentId;
      }
      XMLHttpRequest = require("xmlhttprequest").XMLHttpRequest;
      xhr = new XMLHttpRequest();
      xhr.setDisableHeaderCheck(true);
      xhr.open('POST', site.full_url + "/blogs", true);
      xhr.setRequestHeader("Content-Type", "application/json");
      xhr.setRequestHeader("Accept", "application/vnd.moxiesoft.spaces-v1+json");
      xhr.setRequestHeader("Cookie", ["_social_navigator_session=" + Spaces.Session.getSessionId(userId), "path=/"]);
      xhr.onload = function() {
        var id;
        if (this.status === 201) {
          id = this.responseText.match(/.*"id":"(.*?)".*/)[1];
          return Spaces.Session.addItem(site, userId, 'blog', id);
        } else {
          return console.log("Blog.create ERROR: " + this.status + " - " + this.responseText);
        }
      };
      return xhr.send(JSON.stringify(data));
    };

    Blog.createPost = function(site, blogId, userId) {
      var Spaces, XMLHttpRequest, data, xhr;
      Spaces = require('./spaces-client.js');
      data = {
        blog_post: {
          title: Faker.Lorem.sentences(1),
          body: Faker.Lorem.sentences(20),
          parent_id: blogId
        }
      };
      XMLHttpRequest = require("xmlhttprequest").XMLHttpRequest;
      xhr = new XMLHttpRequest();
      xhr.setDisableHeaderCheck(true);
      xhr.open('POST', site.full_url + "/blog_posts", true);
      xhr.setRequestHeader("Content-Type", "application/json");
      xhr.setRequestHeader("Accept", "application/vnd.moxiesoft.spaces-v1+json");
      xhr.setRequestHeader("Cookie", ["_social_navigator_session=" + Spaces.Session.getSessionId(userId), "path=/"]);
      xhr.onload = function() {
        var id;
        if (this.status === 201) {
          id = this.responseText.match(/.*"id":"(.*)".*/)[1];
          return Spaces.Session.addItem(site, userId, 'blogpost', id);
        } else {
          return console.log("BlogPost.create ERROR: " + this.status + " - " + JSON.stringify(data));
        }
      };
      return xhr.send(JSON.stringify(data));
    };

    return Blog;

  })();

  module.exports = exports = Blog;

}).call(this);
