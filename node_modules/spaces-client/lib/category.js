// Generated by CoffeeScript 1.6.3
(function() {
  var Category, exports;

  Category = (function() {
    var Faker;

    function Category() {}

    Faker = require('Faker');

    Category.create = function(site, parentId, userId) {
      var Spaces, XMLHttpRequest, data, xhr;
      Spaces = require('./spaces-client.js');
      data = {
        category: {
          title: Faker.Lorem.sentences(1),
          body: Faker.Lorem.sentences(4)
        }
      };
      if (parentId) {
        data['category']['parent_id'] = parentId;
      }
      XMLHttpRequest = require("xmlhttprequest").XMLHttpRequest;
      xhr = new XMLHttpRequest();
      xhr.setDisableHeaderCheck(true);
      xhr.open('POST', site.full_url + "/categories", true);
      xhr.setRequestHeader("Content-Type", "application/json");
      xhr.setRequestHeader("Accept", "application/vnd.moxiesoft.spaces-v1+json");
      xhr.setRequestHeader("Cookie", ["_social_navigator_session=" + Spaces.Session.getSessionId(userId), "path=/"]);
      xhr.onload = function() {
        var id;
        if (this.status === 201) {
          id = this.responseText.match(/.*"id":"(.*?)".*/)[1];
          return Spaces.Session.addItem(site, userId, 'category', id);
        } else {
          return console.log("Category.create ERROR: " + this.status + " - " + this.responseText);
        }
      };
      return xhr.send(JSON.stringify(data));
    };

    return Category;

  })();

  module.exports = exports = Category;

}).call(this);
