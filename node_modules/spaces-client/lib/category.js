// Generated by CoffeeScript 1.6.3
(function() {
  var Category, exports;

  Category = (function() {
    var Faker, Resource;

    function Category() {}

    Faker = require('Faker');

    Resource = require("./resource");

    Category.showCategory = function(site, sessionId, resourceId, parentId, userId, onsuccess, onfail) {
      var addlHeaders;
      addlHeaders = {
        "Cookie": ["_social_navigator_session=" + sessionId, "path=/"]
      };
      return Resource.getResource(site, userId, sessionId, 'categories/' + resourceId, addlHeaders, (function(res) {
        return onsuccess();
      }), (function() {
        return onfail();
      }));
    };

    Category.createCategory = function(site, sessionId, resourceId, parentId, userId, onsuccess, onfail, oncreate) {
      var data;
      data = {
        category: {
          title: Faker.Lorem.sentences(1),
          body: Faker.Lorem.sentences(4)
        }
      };
      if (parentId) {
        data['category']['parent_id'] = parentId;
      }
      return Resource.createResource(site, userId, sessionId, 'categories', data, onsuccess, onfail, oncreate);
    };

    return Category;

  })();

  module.exports = exports = Category;

}).call(this);
