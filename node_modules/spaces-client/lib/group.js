// Generated by CoffeeScript 1.6.3
(function() {
  var Group, exports;

  Group = (function() {
    var Faker, Resource;

    function Group() {}

    Faker = require('Faker');

    Resource = require("./resource");

    Group.createGroup = function(site, sessionId, resourceId, parentId, userId, onsuccess, onfail, oncreate) {
      var data;
      data = {
        group: {
          _type: "BasicGroup",
          title: Faker.Lorem.sentences(1),
          body: Faker.Lorem.sentences(4),
          parent_id: parentId
        }
      };
      return Resource.createResource(site, userId, sessionId, 'groups', data, onsuccess, onfail, oncreate);
    };

    Group.showGroup = function(site, sessionId, resourceId, parentId, userId, onsuccess, onfail) {
      var addlHeaders;
      addlHeaders = {
        "Accept": "application/vnd.moxiesoft.spaces-v1+json",
        "Cookie": ["_social_navigator_session=" + sessionId, "path=/"]
      };
      return Resource.getResource(site, userId, sessionId, 'groups/' + resourceId, addlHeaders, (function(res) {
        return onsuccess();
      }), (function() {
        return onfail();
      }));
    };

    return Group;

  })();

  module.exports = exports = Group;

}).call(this);
