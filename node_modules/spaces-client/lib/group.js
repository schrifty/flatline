// Generated by CoffeeScript 1.6.3
(function() {
  var Group, exports;

  Group = (function() {
    var Faker;

    function Group() {}

    Faker = require('Faker');

    Group.create = function(site, parentId, userId) {
      var Spaces, XMLHttpRequest, data, xhr;
      Spaces = require('./spaces-client.js');
      data = {
        group: {
          title: Faker.Lorem.sentences(1),
          body: Faker.Lorem.sentences(4)
        }
      };
      if (parentId) {
        data['group']['parent_id'] = parentId;
      }
      XMLHttpRequest = require("xmlhttprequest").XMLHttpRequest;
      xhr = new XMLHttpRequest();
      xhr.setDisableHeaderCheck(true);
      xhr.open('POST', site.full_url + "/groups", true);
      xhr.setRequestHeader("Content-Type", "application/json");
      xhr.setRequestHeader("Accept", "application/vnd.moxiesoft.spaces-v1+json");
      xhr.setRequestHeader("Cookie", ["_social_navigator_session=" + Spaces.Session.getSessionId(userId), "path=/"]);
      xhr.onload = function() {
        var id;
        if (this.status === 201) {
          id = this.responseText.match(/.*"id":"(.*?)".*/)[1];
          return Spaces.Session.addItem(site, userId, 'group', id);
        } else {
          return console.log("Group.create ERROR: " + this.status + " - " + this.responseText);
        }
      };
      return xhr.send(JSON.stringify(data));
    };

    return Group;

  })();

  module.exports = exports = Group;

}).call(this);
