// Generated by CoffeeScript 1.6.3
(function() {
  var $, Faker, Group, exports, logger;

  Faker = require('Faker');

  logger = require('./logger.js');

  $ = require("jquery");

  Group = (function() {
    function Group() {}

    Group.create = function(site, parentId, userId) {
      var Spaces, XMLHttpRequest, data, xhr;
      Spaces = require('./spaces-client.js');
      data = {
        group: {
          _type: "BasicGroup",
          title: Faker.Lorem.sentences(1),
          body: Faker.Lorem.sentences(4),
          parent_id: parentId
        }
      };
      data = decodeURIComponent($.param(data));
      XMLHttpRequest = require("xmlhttprequest").XMLHttpRequest;
      xhr = new XMLHttpRequest();
      xhr.setDisableHeaderCheck(true);
      xhr.open('POST', site.full_url + "/groups", true);
      xhr.setRequestHeader("Accept", "application/vnd.moxiesoft.spaces-v1+json");
      xhr.setRequestHeader("Cookie", ["_social_navigator_session=" + Spaces.Session.getSessionId(site, userId), "path=/"]);
      xhr.onload = function() {
        var id;
        if (this.status === 201) {
          id = this.responseText.match(/.*"id":"(.*?)".*/)[1];
          return Spaces.Session.addItem(site, userId, 'group', id);
        } else {
          return logger.error("[%s][%s] Group.create failed: %s - %s - %j", site.site_id, userId, this.status, this.responseText, data);
        }
      };
      return xhr.send(JSON.stringify(data));
    };

    return Group;

  })();

  module.exports = exports = Group;

}).call(this);
