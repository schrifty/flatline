// Generated by CoffeeScript 1.6.3
(function() {
  var Pod;

  Pod = (function() {
    function Pod() {}

    Pod.getPods = function(site, onsuccess) {
      var headers;
      headers = {
        "API-KEY": Spaces.apiKey
      };
      return Resource.getResource(host, "system", null, '/api/pods', headers, (function(pods) {
        var pod;
        pod = $.grep(pods, (function(pod) {
          return pod.id === site.pod_id;
        }));
        return onsuccess(pod.app_servers.length, pod.job_servers.length);
      }));
    };

    return Pod;

  })();

}).call(this);
