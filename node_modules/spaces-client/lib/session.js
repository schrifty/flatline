// Generated by CoffeeScript 1.6.3
(function() {
  var Session, exports;

  Session = (function() {
    function Session() {}

    Session.sessions = {};

    Session.setSessionId = function(userId, id) {
      if (!this.sessions[userId]) {
        this.sessions[userId] = {};
      }
      this.sessions[userId]["id"] = id;
      return console.log("Stored session ID [" + this.sessions[userId]["id"] + "] for user [" + userId + "]");
    };

    Session.getSessionId = function(userId) {
      var session, sessionId;
      if (session = this.sessions[userId]) {
        if (sessionId = session["id"]) {
          return sessionId;
        } else {
          return console.log("Couldn't find ID within session for user [" + userId + "]");
        }
      } else {
        return console.log("Couldn't find session for user [" + userId + "]");
      }
    };

    Session.getRandomItemIdOfType = function(userId, type) {
      var list;
      if (this.sessions[userId]) {
        if (list = this.sessions[userId][type]) {
          return list[Math.floor(Math.random() * list.length)];
        } else {
          return console.log("There are NO blogs");
        }
      } else {
        return console.log("ERROR: user session not found");
      }
    };

    Session.addItem = function(userId, type, item) {
      console.log("Adding " + type + " [" + item + "] for user [" + userId + "]");
      if (this.sessions[userId]) {
        if (!this.sessions[userId][type]) {
          this.sessions[userId][type] = [];
        }
        return this.sessions[userId][type].push(item);
      }
    };

    return Session;

  })();

  module.exports = exports = Session;

}).call(this);
